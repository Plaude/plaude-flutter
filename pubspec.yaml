name: plaude_flutter
description: "The Plaude's Flutter SDK."
version: 1.0.0
homepage: https://github.com/plaude/plaude-flutter

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
